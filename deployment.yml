apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: demo-deployment
spec:
  selector:
    matchLabels: 
      app: demo
  template:
    metadata:
      labels:
        app: demo
    spec:
      containers: 
    - name: demo
      image: demo:1.0
      containerPort: 8080
